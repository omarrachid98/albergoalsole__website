export interface ServicesTypes {
  id: number;
  title: string;
  description: string;
  icon?: string;
}

export interface ContactsFormTypes {
  name: string;
  email: string;
  surname: string;
  message: string;
}
