export interface ServicesTypes {
    id: number;
    title: string;
    description: string;
}

export interface ContactsFormTypes {
    name: string;
    email: string;
    surname: string;
    //tel
    message: string;
}